UI PASS 2 — DEVELOPER INSTRUCTIONS
Date: 24.08.2026
Branch: ui-pass-2-20260823

// DEVLOG. WHAT DID I DO. (SHOULD NOT BE EDITED BY THE AI)
25.08: Created a new Node in Main called Ui (Type: Control) (Size: 720x1280)
25.08: Put the existing TopUi (Control) and BottomUi (Control) Nodes into this new Node
25.08: Created a new Node in Ui called WheelUi (Control)
25.08: Created a new Node (Node2D) in Ui called StatisticsWindow, and added the newly created Scene "StatisticsWindow.tscn" to it.
25.08: Created a new Node (Node2D) in Ui called SettingsWindow and added the newly created Scene "SettingsWindow.tscn" to it.
25.08: Created a new Node (Node2D) in Ui called BuyWindow and added the newly created Scene "BuyWindow.tscn" to it.
25.08: Created a new Node (Node2D) in Ui called UpgradeWindow and added the newly created Scene "UpgradeWindow.tscn" to it.
25.08: I moved the existing UiWindowManager node (Node) into the Ui Node.
25.08: Renamed BottomUI to BottomUi and TopUI to TopUi (Lowercase i)
25.08: Created a new Script "TopUi.cs" and attached it to the TopUi Node.
25.08: Created a new Script "BottomUi.cs" and attached it to the BottomUi Node.
25.08: BottomUi already has StatisticsButton, SellEnergyButton, SetingsButton.
25.08: Created a new Node in BottomUi called PrestigeButton. (Control) and added a new Script to it "PrestigeButton.cs"
25.08: Created a new Script "StatisticsButton.cs" and attached it to the StatisticsButton Node in BottomUi.
25.08: Created a new Script "SettingsButton.cs" and attached it to the SettingsButton Node in BottomUi.
!! -- We will have to Recycle "UiToggleButton.cs" -- !!
!! -- We also have to remove res://Scripts/ui/StatisticsGraphUiSettings.cs because it does not seem to be needed. -- !!
26.08:

============================================================
0. GODOT SCENE (.tscn) DECISION — READ THIS FIRST
============================================================

The UI architecture must stay simple. Do NOT create a separate .tscn for every UI Node or every visual element.

For the current UI work, these are the ONLY separate UI scene types that should be created:

1. Main.tscn
   Existing main game scene. This remains the overall composition root.

2. WheelDisplay.tscn
   Reusable scene for ONE wheel's UI display.
   It is instantiated six times, once for each logical wheel.

3. BuyWindow.tscn
   Self-contained Buy Wheel modal window.

4. UpgradeWindow.tscn
   Self-contained Upgrade Wheel modal window.

5. SettingsWindow.tscn
   Self-contained Settings modal window.

6. StatisticsWindow.tscn
   Self-contained Statistics modal window containing its graph Nodes.

These six scene types are sufficient for the current UI Pass 2 architecture.

IMPORTANT: Do NOT create separate scenes for Ui, TopUi, BottomUi, WheelUi, UiWindowManager, individual graphs, displays, buttons, labels, panels, or layout containers unless a future requirement clearly makes one of them an independently reusable/self-contained component.

SCENE DECISION RULE

Create a separate .tscn only when at least one of these is true:
- The component is a self-contained UI component with its own meaningful layout and behavior.
- The component is a reusable component that will be instantiated multiple times.
- The component is an independently shown/hidden modal or major UI window.
- Keeping it embedded would make the owning scene unnecessarily large or difficult to maintain.

Do NOT create a .tscn merely because a Node has a script, is visually important, or has several child Nodes.

The goal is a clean, understandable hierarchy — not maximum scene fragmentation.

SCENE COMPOSITION

Main.tscn
└── Ui
    ├── TopUi
    ├── BottomUi
    ├── WheelUi
    │   ├── Wheel1  ← WheelDisplay.tscn instance
    │   ├── Wheel2  ← WheelDisplay.tscn instance
    │   ├── Wheel3  ← WheelDisplay.tscn instance
    │   ├── Wheel4  ← WheelDisplay.tscn instance
    │   ├── Wheel5  ← WheelDisplay.tscn instance
    │   └── Wheel6  ← WheelDisplay.tscn instance
    ├── StatisticsWindow  ← StatisticsWindow.tscn instance
    ├── SettingsWindow    ← SettingsWindow.tscn instance
    ├── BuyWindow         ← BuyWindow.tscn instance
    ├── UpgradeWindow     ← UpgradeWindow.tscn instance
    └── UiWindowManager

TopUi, BottomUi and WheelUi are ordinary Nodes inside Main.tscn. They do NOT need their own scenes.

StatisticsGraph and EnergyPerWheelGraph are ordinary child Nodes inside StatisticsWindow.tscn. They do NOT need separate scenes at this stage.

UiWindowManager is an application-level coordinator, not a visual component, and therefore is an ordinary Node inside the UI hierarchy.

UiSettings.cs is a static configuration class and is NOT a scene.

DO NOT create:
- Ui.tscn
- TopUi.tscn
- BottomUi.tscn
- WheelUi.tscn
- StatisticsGraph.tscn
- EnergyPerWheelGraph.tscn
- Scenes for individual Labels, Buttons, Panels, MarginContainers, VBoxContainers, HBoxContainers, spacers, sliders, indicators, graph labels, or similar small UI pieces.

If a proposed new scene is not clearly justified by the rules above, keep the component as a child Node in its owning scene.

============================================================
1. PURPOSE
============================================================

This document defines the Godot Node/Scene architecture to use before refactoring the existing UI scripts.

IMPORTANT
Create the required scene Nodes in the Godot editor first. Do not replace the existing UI in one large rewrite. Migrate existing behavior incrementally.

ARCHITECTURE RULE
A major UI component may have a dedicated script without having its own scene. A separate scene is reserved for reusable, self-contained, or independently managed UI components.

2. MAIN UI HIERARCHY

The existing Main.tscn remains the overall scene.

Its Ui area should contain:
- Ui
  - TopUi
  - BottomUi
  - WheelUi
  - StatisticsWindow
  - SettingsWindow
  - BuyWindow
  - UpgradeWindow
  - UiWindowManager

The exact existing Main.tscn structure should be preserved where possible during migration.

3. TOP UI

Node Type:
Control
Name:
TopUi
Script:
TopUi.cs

Children:
- TopUiBackground
- EnergyDisplay
- DollarsDisplay
- RainAmountDisplay

Purpose:
Own and update persistent top-of-screen information.

TopUi remains an ordinary Node in Main.tscn. Do not create TopUi.tscn.

4. BOTTOM UI

Node Type:
Control
Name:
BottomUi
Script:
BottomUi.cs

Children:
- BuyArea
- UpgradeArea
- SettingsButton
- StatisticsButton
- other persistent bottom controls as required

Purpose:
Own persistent bottom controls and route interaction to the appropriate windows/components.

BottomUi remains an ordinary Node in Main.tscn. Do not create BottomUi.tscn.

5. WHEEL UI

Node Type:
Control
Name:
WheelUi
Script:
WheelUi.cs

WheelUi remains an ordinary Node in Main.tscn. Do not create WheelUi.tscn.

Children:
Wheel1, Wheel2, Wheel3, Wheel4, Wheel5, Wheel6

Each Wheel is an instance of WheelDisplay.tscn.

IMPORTANT NUMBERING RULE
Wheel numbers are logical numbers from left to right:
Wheel1 = 1
Wheel2 = 2
Wheel3 = 3
Wheel4 = 4
Wheel5 = 5
Wheel6 = 6

Do not derive display numbering independently in multiple scripts.

6. WHEEL DISPLAY SCENE

Scene:
WheelDisplay.tscn
Root Node:
Control named WheelDisplay
Script:
WheelDisplay.cs

This scene contains the visual Nodes needed for one wheel display, including its Buy/Upgrade interaction controls as appropriate.

The same scene is instantiated six times. The wheel number is instance data, not a reason to create six different scenes.

7. BUY WINDOW

Scene:
BuyWindow.tscn
Root Node:
PanelContainer named BuyWindow
Script:
BuyWindow.cs

Children:
- MarginContainer
  - VBoxContainer
    - TitleLabel
    - WheelNumberLabel
    - UnlockRow
      - UnlockLabel
      - PriceButton
    - Spacer
    - CloseWindowButton

TitleLabel:
Text: Buy Wheel

WheelNumberLabel:
Displays the logical wheel number, e.g. Wheel 1.

UnlockLabel:
Text: Unlock Wheel

PriceButton:
Text: 10$

BuyWindow.cs controls these existing Nodes. It must not construct the complete window hierarchy at runtime.

8. UPGRADE WINDOW

Scene:
UpgradeWindow.tscn
Root Node:
PanelContainer named UpgradeWindow
Script:
UpgradeWindow.cs

Children:
- MarginContainer
  - VBoxContainer
    - TitleLabel
    - WheelNumberLabel
    - UpgradeList
    - CloseWindowButton

WheelNumberLabel displays the logical wheel number.

UpgradeWindow.cs controls existing upgrade rows/buttons instead of dynamically constructing the complete window.

9. SETTINGS WINDOW

Scene:
SettingsWindow.tscn
Root Node:
PanelContainer named SettingsWindow
Script:
SettingsWindow.cs

Children:
- MarginContainer
  - VBoxContainer
    - SettingsTitle
    - TiltSettingsArea
    - TiltSlider
    - GravityIndicator
    - additional existing settings controls
    - CloseWindowButton

SettingsWindow.cs owns settings interaction. Shared visual styling comes from UiSettings.cs.

10. STATISTICS WINDOW

Scene:
StatisticsWindow.tscn
Root Node:
PanelContainer named StatisticsWindow
Script:
StatisticsWindow.cs

Children:
- MarginContainer
  - VBoxContainer
    - StatisticsHeader
    - ExistingGraph
    - RainAmountGraph
    - EnergyPerWheelGraph
    - CloseWindowButton

The graph components are ordinary child Nodes in StatisticsWindow.tscn. They do not require separate .tscn files at this stage.

StatisticsGraph.cs may control the standard graph Nodes, and EnergyPerWheelGraph.cs may control the bar-based graph. Scripts and scenes are separate architectural decisions.

11. WINDOW / MODAL COORDINATION

Node Type:
Node
Name:
UiWindowManager
Script:
UiWindowManager.cs

Place it directly inside the existing Ui hierarchy in Main.tscn.

It is NOT a separate scene.

Responsibilities:
- Track the active modal window.
- Ensure modal windows are in front.
- Close the active modal when clicking/touching outside it where appropriate.
- Prevent multiple modal windows from fighting for input.

Individual windows remain responsible for their own internal behavior.

12. UI SETTINGS

File:
UiSettings.cs

UiSettings remains a centralized static configuration class.

All shared values such as:
- BorderColor
- BorderSize
- ButtonBorderSize
- Button colors
- Enabled/disabled font colors
- Font sizes

must come from UiSettings.cs.

Do not introduce local copies of these values inside individual UI scripts unless a component has a deliberate unique visual requirement.

13. SCRIPT RESPONSIBILITY RULES

UI scripts should:
- Find/cache their child Nodes.
- Update text and visual state.
- Connect signals/events.
- React to game state.
- Position their own component when necessary.
- Request modal/window changes from UiWindowManager when appropriate.

UI scripts should NOT:
- Build an entire UI hierarchy from scratch.
- Duplicate shared UiSettings constants.
- Own unrelated windows.
- Duplicate wheel numbering logic.
- Duplicate global modal input handling.

A script does NOT automatically require a separate .tscn. Prefer ordinary child Nodes when a component is not reusable or independently managed.

14. RECOMMENDED MIGRATION ORDER

1. Preserve Main.tscn as the overall composition root.
2. Add/organize UiWindowManager as a Node in the existing UI hierarchy.
3. Create WheelDisplay.tscn and instance it six times under WheelUi.
4. Create BuyWindow.tscn and attach BuyWindow.cs.
5. Migrate the current working Buy Window behavior.
6. Create UpgradeWindow.tscn and attach UpgradeWindow.cs.
7. Migrate the current working Upgrade Window behavior.
8. Create StatisticsWindow.tscn and organize its graph child Nodes.
9. Migrate SettingsWindow into SettingsWindow.tscn.
10. Migrate TopUi and BottomUi as ordinary Nodes; do not create scenes for them.
11. Remove obsolete runtime UI construction only after each migration is verified.

15. TESTING REQUIREMENT

After every migration:
- Run on Windows.
- Verify mouse interaction.
- Verify outside-click behavior.
- Verify window layering.
- Verify enabled/disabled colors.
- Verify no duplicate UI is being created.

After the complete migration:
- Test Android touch input.
- Verify all six wheel numbers.
- Verify Buy and Upgrade windows display the correct wheel number.
- Verify all statistics graphs.

END OF DEVELOPER INSTRUCTIONS
