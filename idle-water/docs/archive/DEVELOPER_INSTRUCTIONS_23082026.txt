UI PASS 2 — DEVELOPER INSTRUCTIONS
Date: 28.08.2026
Branch: ui-pass-2-20260823

// DEVLOG. WHAT DID I DO. (SHOULD NOT BE EDITED BY THE AI)
25.08: Created a new Node in Main called Ui (Type: Control) (Size: 720x1280)
25.08: Put the existing TopUi (Control) and BottomUi (Control) Nodes into this new Ui Node.
25.08: Created a new Node in Ui called WheelUi (Control).
25.08: Created StatisticsWindow, SettingsWindow, BuyWindow and UpgradeWindow scenes and placed their instances in Ui.
25.08: Moved the existing UiWindowManager node into the Ui Node.
25.08: Renamed BottomUI to BottomUi and TopUI to TopUi (lowercase i).
25.08: Created TopUi.cs and attached it to TopUi.
25.08: Created BottomUi.cs and attached it to BottomUi.
25.08: BottomUi already has StatisticsButton, SellEnergyButton and SettingsButton.
25.08: Created PrestigeButton (Control) in BottomUi and attached PrestigeButton.cs.
25.08: Created StatisticsButton.cs and attached it to StatisticsButton.
25.08: Created SettingsButton.cs and attached it to SettingsButton.
!! -- We will have to recycle UiToggleButton.cs. -- !!
!! -- We also have to remove res://Scripts/ui/StatisticsGraphUiSettings.cs because it does not seem to be needed. -- !!
28.08: Created WheelDisplay.tscn, attached WheelDisplay.cs and instanced it 6 times in WheelUi.
28.08: Created PrestigeWindow.tscn as a Control-based modal window and placed it in CenterUi.
28.08: Created TopUiBackground in TopUi.
!! -- TopUi already has RainDisplayContainer (Type: Node) with RainDisplay, and CurrenciesContainer with EnergyDisplay and MoneyDisplay. We will keep them. -- !!
28.08: Created and attached RainDisplay.cs, EnergyDisplay.cs and MoneyDisplay.cs.
28.08: Created WheelUi.cs and attached it to WheelUi.
28.08: Created a VBoxContainer in StatisticsWindow and added Graph1, Graph2 and Graph3 child Nodes.
28.08: Created and attached a script for each graph Node in res://Scripts/ui/graphs/.
28.08: Created SettingsContent (VBoxContainer) in SettingsWindow with TiltSettingsContent, AudioSettings and MoreInfoButton children.
28.08: Attached SettingsContent.cs to SettingsContent. Most of its old responsibilities will be migrated to the child scripts.
28.08: Created and attached TiltSettingsContent.cs, AudioSettings.cs and MoreInfoButton.cs.
28.08: Made sure all Windows and Scenes have their appropriate scripts attached.
28.08: Created res://Scripts/global and moved RenderedWindowBackground.cs and UiSettings.cs there.
28.08: Created RenderedButtonBackground.cs in global so shared button background rendering is centralized.
28.08: Created ButtonContainer in WheelDisplay.tscn with no script.
28.08: Created BuyButton and UpgradeButton Nodes inside ButtonContainer and attached BuyButton.cs and UpgradeButton.cs.
!! -- There were two files named TiltSettings.cs. The newer one was renamed to TiltSettingsContent.cs. -- !!
28.08: Renamed the SettingsWindow Node to TiltSettingsContent.
28.08: Deleted the old CenterUi content.
28.08: Moved CenterUi to be a child of Ui.
28.08: Moved StatisticsWindow, SettingsWindow and PrestigeWindow under CenterUi.
28.08: Created CenterUi.cs and attached it to CenterUi.
28.08: Made StatisticsWindow, SettingsWindow and PrestigeWindow Control Nodes.
!! -- Current runtime notes: SellEnergyButton is still visible; Tilt Settings appear in the top-left; old Buy and Upgrade Buttons/Windows still work; TopUi is visible; Settings and Statistics Buttons are currently not visible. These are migration issues to resolve, not reasons to change the target architecture. -- !!


============================================================
0. GODOT SCENE (.tscn) DECISION — READ THIS FIRST
============================================================

The UI architecture must stay simple. Do NOT create a separate .tscn for every UI Node or every visual element.

For the current UI Pass 2 architecture, these are the approved separate UI scene types:

1. Main.tscn
   Existing main game scene. This remains the overall composition root.

2. WheelDisplay.tscn
   Reusable scene for ONE wheel's UI display. It is instantiated six times.

3. BuyWindow.tscn
   Self-contained Buy Wheel modal window.

4. UpgradeWindow.tscn
   Self-contained Upgrade Wheel modal window.

5. SettingsWindow.tscn
   Self-contained Settings modal window.

6. StatisticsWindow.tscn
   Self-contained Statistics modal window containing its graph Nodes.

7. PrestigeWindow.tscn
   Self-contained Prestige modal window.

These seven scene types are sufficient for the current UI Pass 2 architecture.

IMPORTANT: Do NOT create separate scenes for Ui, TopUi, BottomUi, WheelUi, CenterUi, UiWindowManager, individual graphs, displays, buttons, labels, panels, or layout containers unless a future requirement clearly makes one of them an independently reusable/self-contained component.

SCENE DECISION RULE

Create a separate .tscn only when at least one of these is true:
- The component is a self-contained UI component with its own meaningful layout and behavior.
- The component is a reusable component that will be instantiated multiple times.
- The component is an independently shown/hidden modal or major UI window.
- Keeping it embedded would make the owning scene unnecessarily large or difficult to maintain.

Do NOT create a .tscn merely because a Node has a script, is visually important, or has several child Nodes.

The goal is a clean, understandable hierarchy — not maximum scene fragmentation.

SCENE COMPOSITION

Main.tscn
└── Ui
    ├── TopUi
    ├── BottomUi
    ├── WheelUi
    │   ├── Wheel1  ← WheelDisplay.tscn instance
    │   ├── Wheel2  ← WheelDisplay.tscn instance
    │   ├── Wheel3  ← WheelDisplay.tscn instance
    │   ├── Wheel4  ← WheelDisplay.tscn instance
    │   ├── Wheel5  ← WheelDisplay.tscn instance
    │   └── Wheel6  ← WheelDisplay.tscn instance
    ├── CenterUi
    │   ├── StatisticsWindow  ← StatisticsWindow.tscn instance
    │   ├── SettingsWindow    ← SettingsWindow.tscn instance
    │   └── PrestigeWindow     ← PrestigeWindow.tscn instance
    ├── BuyWindow              ← BuyWindow.tscn instance
    ├── UpgradeWindow          ← UpgradeWindow.tscn instance
    └── UiWindowManager

CenterUi is an ordinary Control Node in Main.tscn. It groups the centered modal windows and owns their common positioning/layering as appropriate. It does NOT need its own scene.

TopUi, BottomUi and WheelUi are ordinary Nodes inside Main.tscn. They do NOT need their own scenes.

StatisticsWindow.tscn contains the graph Nodes. Individual graph Nodes do NOT need separate scenes at this stage.

UiWindowManager is an application-level coordinator, not a visual component, and therefore is an ordinary Node inside the UI hierarchy.

UiSettings.cs is a static configuration class and is NOT a scene.

DO NOT create:
- Ui.tscn
- TopUi.tscn
- BottomUi.tscn
- WheelUi.tscn
- CenterUi.tscn
- StatisticsGraph.tscn
- EnergyPerWheelGraph.tscn
- Graph1.tscn
- Graph2.tscn
- Graph3.tscn
- Scenes for individual Labels, Buttons, Panels, MarginContainers, VBoxContainers, HBoxContainers, spacers, sliders, indicators, graph labels, or similar small UI pieces.

If a proposed new scene is not clearly justified by the rules above, keep the component as a child Node in its owning scene.

============================================================
1. PURPOSE
============================================================

This document defines the Godot Node/Scene architecture to use during the UI script migration.

IMPORTANT
The scene structure has already been created in Godot. Do not replace the existing UI in one large rewrite. Migrate existing behavior incrementally.

ARCHITECTURE RULE
A major UI component may have a dedicated script without having its own scene. A separate scene is reserved for reusable, self-contained, or independently managed UI components.

============================================================
2. MAIN UI HIERARCHY
============================================================

The existing Main.tscn remains the overall scene.

Its Ui area should contain:
- Ui
  - TopUi
  - BottomUi
  - WheelUi
    - Wheel1
    - Wheel2
    - Wheel3
    - Wheel4
    - Wheel5
    - Wheel6
  - CenterUi
    - StatisticsWindow
    - SettingsWindow
    - PrestigeWindow
  - BuyWindow
  - UpgradeWindow
  - UiWindowManager

The exact existing Main.tscn structure should be preserved during migration. Do not restructure unrelated simulation Nodes.

============================================================
3. TOP UI
============================================================

Node Type:
Control
Name:
TopUi
Script:
TopUi.cs

Current children include:
- TopUiBackground
- RainDisplayContainer
  - RainDisplay
- CurrenciesContainer
  - EnergyDisplay
  - MoneyDisplay

Purpose:
Own persistent top-of-screen UI layout and coordinate its display components.

Display scripts:
- RainDisplay.cs controls RainDisplay.
- EnergyDisplay.cs controls EnergyDisplay.
- MoneyDisplay.cs controls MoneyDisplay.

Do not rename MoneyDisplay to DollarsDisplay during migration unless the Node is intentionally renamed in Godot first. Keep code and Node names synchronized.

TopUi remains an ordinary Node in Main.tscn. Do not create TopUi.tscn.

============================================================
4. BOTTOM UI
============================================================

Node Type:
Control
Name:
BottomUi
Script:
BottomUi.cs

Current important children include:
- StatisticsButton
- SellEnergyButton
- SettingsButton
- PrestigeButton
- existing Buy/Upgrade areas or controls that are still being migrated

Button scripts:
- StatisticsButton.cs
- SettingsButton.cs
- PrestigeButton.cs
- existing SellEnergyButton.cs

Purpose:
Own persistent bottom controls and route interaction to the appropriate windows/components.

BottomUi remains an ordinary Node in Main.tscn. Do not create BottomUi.tscn.

============================================================
5. WHEEL UI
============================================================

Node Type:
Control
Name:
WheelUi
Script:
WheelUi.cs

WheelUi remains an ordinary Node in Main.tscn. Do not create WheelUi.tscn.

Children:
Wheel1, Wheel2, Wheel3, Wheel4, Wheel5, Wheel6

Each Wheel is an instance of WheelDisplay.tscn.

IMPORTANT NUMBERING RULE
Wheel numbers are logical numbers from left to right:
Wheel1 = 1
Wheel2 = 2
Wheel3 = 3
Wheel4 = 4
Wheel5 = 5
Wheel6 = 6

WheelUi is the single owner of wheel ordering/index assignment. WheelDisplay must receive or expose its logical wheel number rather than independently guessing its number.

Do not derive display numbering independently in BuyWindow, UpgradeWindow, WheelDisplay and other scripts.

============================================================
6. WHEEL DISPLAY SCENE
============================================================

Scene:
WheelDisplay.tscn
Root Node:
Control named WheelDisplay
Script:
WheelDisplay.cs

This scene contains the visual Nodes needed for one wheel display.

Current structure includes:
- ButtonContainer
  - BuyButton
  - UpgradeButton

ButtonContainer has no script.
BuyButton uses BuyButton.cs.
UpgradeButton uses UpgradeButton.cs.

The same WheelDisplay.tscn is instantiated six times. The wheel number is instance data, not a reason to create six different scenes.

WheelDisplay.cs should control the existing Nodes belonging to one wheel and should not construct an entire wheel UI hierarchy at runtime.

============================================================
7. BUY WINDOW
============================================================

Scene:
BuyWindow.tscn
Root Node:
Control / PanelContainer-based modal root as currently implemented
Script:
BuyWindow.cs

The exact child hierarchy in the scene is authoritative. BuyWindow.cs must control existing Nodes rather than constructing the complete window hierarchy at runtime.

The window must display the logical wheel number supplied by the wheel system.

Expected examples:
- Buy Wheel 1
- Buy Wheel 2

Do not duplicate wheel-number calculation inside BuyWindow.cs.

============================================================
8. UPGRADE WINDOW
============================================================

Scene:
UpgradeWindow.tscn
Root Node:
Control / PanelContainer-based modal root as currently implemented
Script:
UpgradeWindow.cs

The exact child hierarchy in the scene is authoritative. UpgradeWindow.cs must control existing upgrade Nodes/buttons rather than dynamically constructing the complete window hierarchy.

The window must display the logical wheel number supplied by the wheel system.

Expected examples:
- Upgrade Wheel 1
- Upgrade Wheel 2

Do not duplicate wheel-number calculation inside UpgradeWindow.cs.

============================================================
9. CENTER UI
============================================================

Node Type:
Control
Name:
CenterUi
Script:
CenterUi.cs

CenterUi is an ordinary Node in Main.tscn, not a separate scene.

Children:
- StatisticsWindow
- SettingsWindow
- PrestigeWindow

Purpose:
- Group centered modal/content windows.
- Provide a common parent for their positioning and layering where appropriate.
- Keep centered windows separate from persistent TopUi, BottomUi and WheelUi.

CenterUi must not take over the internal responsibilities of the individual windows. Each window remains responsible for its own content and interaction.

============================================================
10. SETTINGS WINDOW
============================================================

Scene:
SettingsWindow.tscn
Root Node:
Control
Name:
SettingsWindow
Script:
SettingsWindow.cs

The SettingsWindow scene contains its settings content Nodes.

Current settings content includes:
- SettingsContent (VBoxContainer) with SettingsContent.cs
  - TiltSettingsContent
  - AudioSettings
  - MoreInfoButton

TiltSettingsContent.cs owns the migrated Tilt Settings content. AudioSettings.cs owns audio settings. MoreInfoButton.cs owns the More Info interaction.

IMPORTANT
The older SettingsContent.cs may still contain responsibilities from the previous runtime-built UI. Migrate those responsibilities incrementally into the appropriate child scripts. Do not delete working behavior until the replacement has been verified.

SettingsWindow.cs owns the window-level behavior and should not recreate the complete settings hierarchy at runtime.

Shared visual styling comes from UiSettings.cs.

============================================================
11. STATISTICS WINDOW
============================================================

Scene:
StatisticsWindow.tscn
Root Node:
Control
Name:
StatisticsWindow
Script:
StatisticsWindow.cs

Current structure includes a VBoxContainer containing three graph Nodes:
- Graph1
- Graph2
- Graph3

Each graph Node has its own script in:
res://Scripts/ui/graphs/

The graph Nodes are ordinary child Nodes in StatisticsWindow.tscn. They do not require separate .tscn files.

Graph responsibilities must remain separated. A graph script controls its own graph Node and does not construct the entire StatisticsWindow hierarchy.

The third graph is the Energy per Wheel graph and must preserve the planned Wheel 1 → Wheel 6 ordering.

============================================================
12. PRESTIGE WINDOW
============================================================

Scene:
PrestigeWindow.tscn
Root Node:
Control
Name:
PrestigeWindow

PrestigeWindow is a self-contained centered modal window under CenterUi.

It is an approved separate scene because it is an independently shown/hidden major UI window.

Use the existing scene hierarchy as the source of truth when migrating its behavior. Do not build its complete hierarchy dynamically in code.

============================================================
13. WINDOW / MODAL COORDINATION
============================================================

Node Type:
Node
Name:
UiWindowManager
Script:
UiWindowManager.cs

Place it directly inside the existing Ui hierarchy in Main.tscn.

It is NOT a separate scene.

Responsibilities:
- Track the active modal window.
- Ensure modal windows are in front.
- Close the active modal when clicking/touching outside it where appropriate.
- Prevent multiple modal windows from fighting for input.
- Coordinate the modal windows without owning their internal content.

CenterUi may handle common centered-window layout, but UiWindowManager remains responsible for application-level modal state/coordination.

Individual windows remain responsible for their own internal behavior.

============================================================
14. SHARED UI SETTINGS AND RENDERING
============================================================

Files:
- res://Scripts/global/UiSettings.cs
- res://Scripts/global/RenderedWindowBackground.cs
- res://Scripts/global/RenderedButtonBackground.cs

UiSettings remains the centralized static configuration class.

All shared values such as:
- BorderColor
- BorderSize
- ButtonBorderSize
- Button colors
- Enabled/disabled font colors
- Font sizes

must come from UiSettings.cs.

RenderedWindowBackground.cs and RenderedButtonBackground.cs provide centralized rendering/background behavior where appropriate.

Do not introduce local copies of shared visual constants inside individual UI scripts unless a component has a deliberate unique visual requirement.

============================================================
15. SCRIPT RESPONSIBILITY RULES
============================================================

UI scripts should:
- Find/cache their child Nodes.
- Update text and visual state.
- Connect signals/events.
- React to game state.
- Position their own component when necessary.
- Request modal/window changes from UiWindowManager when appropriate.
- Communicate with their parent/controller through explicit references or methods rather than recreating other UI components.

UI scripts should NOT:
- Build an entire UI hierarchy from scratch.
- Duplicate shared UiSettings constants.
- Own unrelated windows.
- Duplicate wheel numbering logic.
- Duplicate global modal input handling.
- Recreate Nodes that already exist in a .tscn.

A script does NOT automatically require a separate .tscn. Prefer ordinary child Nodes when a component is not reusable or independently managed.

============================================================
16. MIGRATION ORDER
============================================================

Migrate incrementally and keep the old implementation only as long as needed for safe transition.

1. Verify the current Godot Node hierarchy and script attachments.
2. Migrate WheelDisplay.cs to control the existing WheelDisplay Nodes.
3. Migrate BuyButton.cs and UpgradeButton.cs to control their existing Button Nodes.
4. Migrate WheelUi.cs and establish the single source of truth for wheel numbers 1–6.
5. Migrate BuyWindow.cs to the existing BuyWindow scene.
6. Migrate UpgradeWindow.cs to the existing UpgradeWindow scene.
7. Migrate TopUi.cs and its RainDisplay, EnergyDisplay and MoneyDisplay child scripts.
8. Migrate BottomUi.cs and its button scripts.
9. Migrate CenterUi.cs and the centered window positioning/grouping.
10. Migrate SettingsWindow.cs and move remaining settings responsibilities into TiltSettingsContent.cs, AudioSettings.cs and MoreInfoButton.cs as appropriate.
11. Migrate StatisticsWindow.cs and the three graph scripts.
12. Migrate PrestigeWindow behavior.
13. Finalize UiWindowManager.cs so modal coordination is centralized.
14. Remove obsolete runtime UI construction only after each replacement has been verified.
15. Remove obsolete scripts and duplicate styling/constants only after references are gone.

IMPORTANT
Do not perform steps 2–15 as one giant rewrite. Each component must compile and be tested before moving to the next major component.

============================================================
17. MIGRATION SAFETY RULES
============================================================

During migration:
- Do not change simulation behavior.
- Do not change unrelated gameplay logic.
- Do not replace working UI with a parallel second implementation unless needed temporarily for migration.
- Do not delete old runtime construction before the corresponding scene-based implementation is working.
- Do not create duplicate Nodes at runtime when an equivalent Node already exists in the scene.
- Keep Node names and C# references synchronized.
- Prefer explicit NodePath/exported references or stable GetNode paths over fragile searches.

When a migrated component works, remove only the obsolete code belonging to that component.

============================================================
18. TESTING REQUIREMENT
============================================================

After every major migration:
- Run on Windows.
- Verify the game starts without errors.
- Verify mouse interaction.
- Verify outside-click behavior where applicable.
- Verify window layering.
- Verify enabled/disabled colors.
- Verify no duplicate UI is being created.
- Verify the simulation continues to behave normally.

After WheelDisplay/WheelUi migration:
- Verify all six wheel displays exist.
- Verify numbering is Wheel 1 through Wheel 6 from left to right.
- Verify Buy and Upgrade actions target the correct wheel.

After Buy/Upgrade migration:
- Verify the correct wheel number is displayed.
- Verify affordability colors.
- Verify purchase/upgrade behavior remains unchanged.

After Statistics migration:
- Verify all three graphs.
- Verify the Energy-per-Wheel graph contains the six wheels in the correct order.

After Settings migration:
- Verify Tilt Settings position.
- Verify Tilt slider and Gravity Indicator.
- Verify Audio Settings.
- Verify More Info.

After complete migration:
- Test Android touch input.
- Verify all six wheel numbers.
- Verify Buy and Upgrade windows.
- Verify all statistics graphs.
- Verify all settings controls.
- Verify Prestige window.
- Verify no obsolete runtime UI is being constructed.

END OF DEVELOPER INSTRUCTIONS
