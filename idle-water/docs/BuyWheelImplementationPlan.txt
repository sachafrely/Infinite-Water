INFINITE WATER - BUY WHEEL IMPLEMENTATION PLAN
================================================

Date: 20.08.2026
Feature: Buy Wheel system
Status: IMPLEMENTATION STARTED

Purpose
-------
Implement a player-driven wheel purchase system. The game starts with exactly
one active wheel: the THIRD stable wheel location in the map. The other five
locations are unbought and display a clickable "Buy Wheel" control.

Every wheel costs exactly $100. There is no increasing price, no prerequisite
chain, and no requirement to buy wheels in map order.

Core rules
----------
1. Up to six wheel marker locations are discovered.
2. Wheel IDs are stable location IDs, not runtime-list indexes.
3. Wheel 3 is the starting/primary wheel. This is the third wheel from the top
   under the current deterministic top-to-bottom/left-to-right ordering.
4. Initial ownership is:
       Wheel 1 = false
       Wheel 2 = false
       Wheel 3 = true
       Wheel 4 = false
       Wheel 5 = false
       Wheel 6 = false
5. Every unbought wheel costs exactly $100.
6. A purchase is allowed for any currently unbought wheel.
7. If the player has less than $100, nothing changes.
8. No closes the confirmation and leaves Buy Wheel visible.
9. Yes re-checks ownership and money, activates the selected wheel, spends
   $100, closes confirmation, and removes only that wheel's Buy Wheel control.

Architecture
------------
The implementation separates three responsibilities:

WaterWheelManager
    idle-water/scripts/simulation/WaterWheelManager.cs

    - Discovers wheel marker locations without automatically activating them.
    - Uses atlas coordinates X=7, Y=6.
    - Sorts locations deterministically by TileMap Y, then X.
    - Assigns stable IDs 1..6 after sorting.
    - Owns active FluidWheelState instances and WaterWheelVisual instances.
    - Activates one wheel at a time without rebuilding existing wheels.
    - Keeps runtime index 0 reserved for the explicitly activated starting Wheel 3.
    - Continues to own stepping and energy tracking.

WheelPurchaseSystem
    idle-water/scripts/systems/WheelPurchaseSystem.cs

    - Owns per-location purchase state.
    - Defaults Wheel 3 to purchased.
    - Does not use an unlocked-wheel count.
    - Checks the fixed $100 purchase price.
    - Uses EnergySystem.TrySpendDollars() for the economy transaction.
    - Requests activation from WaterWheelManager.
    - Creates/removes purchase controls in the GameView overlay.
    - Does not implement fluid physics.

World purchase UI
    idle-water/scripts/systems/WheelPurchaseSystem.cs

    - WheelPurchaseWorldUi provides the clickable "Buy Wheel" control.
    - The control is attached to Main/GameView, not the FluidSimulation SubViewport.
    - GameView is the visible 720x1160 screen-space container and uses the same
      coordinate space as the simulation camera, so simulation wheel positions
      can be used directly for the overlay controls.
    - The control has an explicit 120x40 pixel rectangle and high Z index.
    - Creation logs identify every wheel and GameView position.

Confirmation UI
    idle-water/scripts/systems/WheelPurchaseSystem.cs

    - WheelPurchaseConfirmationWindow displays:
          Do you want to buy this wheel for 100$?
      with Yes and No buttons.
    - Yes re-checks the economy and wheel state.
    - No only closes the confirmation.
    - Insufficient funds keeps the confirmation open and shows a small
      "Not enough money. You need 100$." message.

Economy
-------
File:
    idle-water/scripts/simulation/EnergySystem.cs

Added:
    public const double WheelPurchaseCost = 100.0;
    public bool TrySpendDollars(double amount)

TrySpendDollars() is atomic from the economy's point of view:
    amount <= 0       -> succeeds without changing dollars
    dollars < amount  -> fails without changing dollars
    dollars >= amount -> subtracts the requested amount and succeeds

The existing 10 Energy -> $1 sale system remains unchanged.

Sell Energy visibility and input
--------------------------------
File:
    idle-water/Scripts/ui/SellEnergyButton.cs

The scene already contains a BottomUI/SellEnergyButton Control. The script now
creates the actual clickable Button as a child of that Control and fills the
existing 240x120 BottomUI slot. This is more reliable than creating a fallback
button at a negative position relative to BottomUI.

The Sell Energy action itself is handled by SellEnergyButton. EconomyUi only
tracks the button's enabled state, preventing two separate click handlers from
selling two chunks for one click.

The selling rule remains unchanged:
    one click -> exactly 10 Energy -> $1

Initialization
--------------
The existing FluidSimulator call to:

    waterWheelManager.CreateWaterWheelsFromEnvironment(...)

is intentionally kept compatible through:

    idle-water/scripts/simulation/WaterWheelManagerLegacyApi.cs

That compatibility bootstrap now:
    1. discovers all wheel locations,
    2. finds Main/GameView as the purchase UI owner,
    3. creates WheelPurchaseSystem,
    4. initializes the purchased starting Wheel 3,
    5. creates Buy Wheel controls for all other discovered locations,
    6. initializes wheel energy tracking.

This avoids placing normal Control UI inside the simulation SubViewport.

Primary-wheel handling
---------------------
The old assumption that runtime wheel index 0 means map Wheel 1 has been
removed from the wheel creation logic.

Wheel 3 is explicitly activated first. It therefore occupies runtime index 0
and remains the primary solver wheel. Later purchases are appended as additional
runtime wheels and do not replace or recreate Wheel 3.

Stable ordering
---------------
Wheel locations are sorted by:
    1. TileMap Y ascending
    2. TileMap X ascending

IDs are assigned after sorting. The current implementation therefore treats
the third location in this stable top-to-bottom/left-to-right ordering as
Wheel 3 and the starting wheel.

In-game verification has confirmed that Wheel 3 is the desired physical wheel.

Persistence
-----------
Current implementation stage:
    - Runtime ownership state exists in WheelPurchaseSystem.
    - A new runtime starts with Wheel 3 purchased.
    - No separate wheel save file/system has been introduced.

Future save integration:
    - Add the six purchase booleans to the project's real GameState/save layer.
    - Load purchase state before wheel runtime initialization.
    - Save immediately after successful purchases or through the normal save
      mechanism.

This intentionally avoids creating a second unrelated save system.

Android/input requirements
--------------------------
The purchase controls use Godot Button/Control input rather than hover-only
behavior, so they are compatible with mouse and touch input. Only the actual
control rectangle consumes input.

Implementation status - 20.08.2026
-----------------------------------
COMPLETED IN THIS IMPLEMENTATION PASS

[x] Added EnergySystem.TrySpendDollars().
[x] Added fixed EnergySystem.WheelPurchaseCost = $100.
[x] Added per-wheel purchase state.
[x] Changed the starting wheel from Wheel 4 to Wheel 3 after in-game feedback.
[x] Confirmed Wheel 3 is the desired physical starting wheel.
[x] Added deterministic wheel-location discovery and stable IDs.
[x] Stopped automatic activation of every discovered wheel.
[x] Added individual wheel activation without rebuilding active wheels.
[x] Preserved the explicitly selected starting wheel as runtime primary wheel.
[x] Added Buy Wheel controls for unpurchased locations.
[x] Moved Buy Wheel controls from the FluidSimulation owner to the GameView
    overlay so they render in the visible screen-space UI layer.
[x] Made Buy Wheel controls explicit 120x40 controls with explicit visibility
    and high Z index.
[x] Added Yes/No purchase confirmation.
[x] Added insufficient-money handling without changing dollars or ownership.
[x] Added compatibility bootstrap so existing FluidSimulator initialization
    continues to enter the new purchase flow.
[x] Reworked SellEnergyButton to create its clickable action Button inside the
    existing BottomUI/SellEnergyButton control.
[x] Prevented EconomyUi from attaching a second Sell Energy click handler.
[x] Updated this document with the implementation state.

NEXT VERIFICATION / FOLLOW-UP WORK
-----------------------------------
[ ] Run the Godot project and verify five Buy Wheel controls are now visible
    at the unpurchased wheel locations.
[ ] Verify the Godot output contains a Buy Wheel UI creation line for every
    unpurchased discovered wheel.
[ ] Verify the Sell Energy button is visible inside the BottomUI middle slot.
[ ] Verify one Sell Energy click sells exactly one 10-Energy chunk.
[ ] Sell energy until at least $100 is available and test an arbitrary wheel.
[ ] Verify exactly $100 is removed after a successful purchase.
[ ] Verify the purchased wheel becomes active without recreating Wheel 3.
[ ] Verify No leaves the Buy Wheel control visible.
[ ] Verify insufficient funds leave dollars and wheel ownership unchanged.
[ ] Verify touch input on Android after the desktop behavior is confirmed.
[ ] Integrate the six ownership values into the real save/GameState system once
    that system is ready.
