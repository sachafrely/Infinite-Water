INFINITE-WATER — UI / WINDOW / GRAPH FIXES
Implementation plan and progress
Date: 2026-08-23
Branch: ui-layering-fixes-20260822

PURPOSE
This is the second attempt at this UI pass. The primary safety requirement is to preserve the existing Settings and Statistics window opening/closing behavior while fixing window layering, input blocking, styling, graph layout, and related UI issues.

INSTRUCTION REVIEW
The requested work is clear enough to implement. I have reordered it into implementation groups so that input/layering behavior is stabilized before visual tuning. This reduces the risk of repeating the previous failure where window layering broke Settings/Statistics behavior.

IMPLEMENTATION ORDER
1. Baseline and safety
   - Inspect current UI scripts, scene node hierarchy, UI settings, window open/close flow, wheel purchase/upgrade flow, graph rendering, and rendered TopUI background.
   - Do not alter the Fluid Simulation.
   - Identify whether existing scene Nodes must be added/reparented or scripts bound in Godot. If so, stop and ask the developer to create/bind them rather than guessing scene structure.

2. Window layering and input ownership (highest priority)
   - Ensure every modal/small window is above the controls that open it.
   - Ensure Buy Wheel confirmation YES cannot be clicked through when an Upgrade button is visually underneath it.
   - Buy and Upgrade windows are mutually exclusive.
   - Clicking empty game space outside a small window closes that small window.
   - Settings and Statistics windows MUST NOT close when clicking outside them. Their existing open/close behavior must be preserved.
   - Clicking on BUY or UPGRADE must close an open SETTINGS or STATISTICS window.
   - Preserve existing Settings and Statistics open/close functions.
   - Android input: use pressed/unpressed states; do not rely on hover.
   - Use a modal/input-blocking layer where needed so visual layering and input hit-testing agree.

3. Buy / Upgrade behavior
   - Buy button remains ALWAYS pressable, regardless of current money, and opens a purchase window.
   - Purchase window contains buying text, a Buy button, $10 cost, and a bottom Close button.
   - Opening Buy closes Upgrade; opening Upgrade closes Buy.
   - The Buy confirmation window must be centered around the Buy button/wheel position in the same way the Upgrade window is centered around its Upgrade button/wheel position.
   - Upgrade button position moves slightly left for horizontal centering on the wheel.
   - Buy and Upgrade buttons become 16 px wider and 16 px higher in total.
   - Upgrade text/color/availability refreshes automatically when money changes.
   - IMPORTANT: Currently the Upgrade button becomes visually dark when the Buy window is open. This is NOT intended behavior and must be fixed.
   - Final intended behavior: the Upgrade button background/color must NOT change when the Buy window is open; only the Upgrade button text may change according to its enabled/disabled/availability state, using the centralized fontColorEnabled/fontColorDisabled rules.
   - TODO: Use the "Ui Size Medium" value from UiSettings.cs for the Buy and Upgrade UI font size.
   - TODO: Keep the Upgrade window at its current good size; do NOT enlarge it.
   - TODO: Make only the Buy window somewhat larger.

4. Centralized UI styling in UiSettings.cs
   - Add/use window background color: middle grey 0.5, 30% transparency as requested (effective opacity 0.7 per the request; resolve implementation semantics carefully rather than changing unrelated alpha behavior).
   - buttonPressed: middle grey 0.6, 0% transparency.
   - buttonUnpressed: middle grey 0.4, 0% transparency.
   - fontColorEnabled: white.
   - fontColorDisabled: black.
   - Standard border: dark grey, 0% transparency, 3 px.
   - All buttons, including the bottom three, use these centralized settings.
   - Rendered TopUI window part uses the same centralized background/border settings.
   - Rain amount graph MUST use UiSettings.cs border width and border color.
   - All relevant scripts read border width from UiSettings.cs instead of local constants.
   - Settings and Statistics buttons use buttonPressed while their windows are open, and therefore are not transparent.

5. TopUI layout and text stability
   - Center the top-left rain amount vertically inside the 60 px TopUI frame. COMPLETED AND TESTED.
   - Fix the top-right Energy/Dollars text flicker when selling energy. COMPLETED AND TESTED.
   - Keep the top-right text position stable when the number changes, e.g. 9 -> 10. COMPLETED AND TESTED.
   - Keep the TopUI background and border driven by UiSettings.cs.

6. Statistics graphs — CURRENT TODO (23.08)
   - [ ] Apply the correct UiSettings.cs border WIDTH to the rain amount graph.
   - [ ] Apply the correct UiSettings.cs border COLOR to the rain amount graph.
   - [ ] Increase the BOTTOM MARGIN of BOTH graphs so there is clearly more room for the bottom text/labels.
   - [ ] Move the bottom text of BOTH graphs approximately 10 px LOWER so it no longer overlaps the graph.
   - [ ] Keep BOTH graphs exactly the SAME SIZE after increasing their bottom margins; adjust layout/available area rather than changing graph dimensions independently.
   - [ ] Restore the left-side graph labels that were moved accidentally: the first graph's "Value" label and the second graph's "Energy" label must be on the LEFT side of their respective graphs again.
   - [ ] Verify the right-side graph information remains correctly positioned and does not overlap the restored left-side labels.
   - [ ] Verify both graphs remain visually aligned and identical in size on Windows and Android.

7. Buy window — CURRENT TODO (23.08)
   - [ ] Make the main BUY button always pressable/clickable; it must open the Buy window even when the player cannot currently afford the purchase.
   - [ ] Make the Buy window follow the same interaction pattern as the Upgrade window.
   - [ ] Buy window must contain the purchase text, a dedicated Buy button, and the $10 price.
   - [ ] Add a Close Window button at the BOTTOM of the Buy window.
   - [ ] Center the Buy window around the BUY button/wheel, using the same positioning logic that centers the Upgrade window around its corresponding Upgrade button/wheel.
   - [ ] Preserve the existing Buy/Upgrade mutual exclusion and click-blocking behavior while implementing the new Buy window layout.
   - [ ] Ensure the Buy window's internal Buy button handles the actual purchase; the main BUY button only opens the window.
   - [ ] Ensure the internal Buy button correctly reflects whether $10 is affordable without making the main BUY button unpressable.

8. Settings layout
   - Move the Tilt Slider further upward; reduce its excessive top margin.

9. Anti-lag feedback
   - Add a Top-Center message during anti-lag cleanup: "FPS low, cleaning up simulation..."
   - The message should be shown only for the cleanup state and must not interfere with normal gameplay input.
   - IMPLEMENTED AND TESTED: developer confirmed the message appears and looks good; it was subsequently moved 120 px downward as requested.
   - Message font size uses UiSettings.FontSizeMedium.
   - Message font color uses UiSettings.FontColorEnabled (white).

10. Verification / feedback checkpoints
   - After the baseline inspection and first safe layering change, report the exact implementation state and ask the developer to test/confirm before proceeding with more invasive scene changes.
   - Repeat feedback checkpoints after Buy/Upgrade modal behavior and after any Settings/Statistics changes.
   - Settings and Statistics must retain their existing open/close behavior; outside-click closing is explicitly NOT desired.
   - Verify that clicking BUY or UPGRADE closes Settings/Statistics while still allowing the clicked Buy/Upgrade control to work normally.
   - Before finalizing, verify that Settings and Statistics still open and close correctly, modal windows block clicks underneath, and no scene-node assumptions were invented.

PROGRESS
[2026-08-23]
- Developer confirmed that the Upgrade window works correctly: its inside interaction remains open and clicking outside successfully closes it on Windows and Android.
- Developer confirmed that the Settings/Statistics windows remain correct: clicking outside does not close them, while BUY/UPGRADE can close them as intended.
- Developer confirmed the Buy-window click blocking is reliable, including when an Upgrade button is underneath it.
- Implemented BUY -> close active Settings/Statistics, then continue with the Buy action.
- Implemented UPGRADE -> close active Settings/Statistics, then continue with the Upgrade action.
- Implemented TopUI resource positioning improvements: rain display is vertically centered in the 60 px TopUI frame, and Energy/Dollars use fixed label widths plus centered vertical positioning to prevent position changes when digit counts change.
- TopUI rendering continues to use UiSettings window/border values.
- Implemented and tested the anti-lag cleanup message; it was moved 120 px downward and centralized to UiSettings medium font size and enabled/white font color.
- CURRENT OPEN WORK from the latest developer feedback: rain amount graph border width/color must come directly from UiSettings.cs; both graph bottom margins must be larger; bottom graph text must move about 10 px lower; both graphs must remain the same size; the left-side "Value" / "Energy" labels must be restored; and the BUY flow must be changed so the main BUY button is always pressable and opens a centered purchase window containing purchase text, a $10 Buy action, and a bottom Close Window button.

[2026-08-22]
- Created branch: ui-layering-fixes-20260822 from main.
- Confirmed the repository contains the relevant UI scripts including UiSettings.cs, SettingsWindow.cs, StatisticsWindow.cs, WheelUpgradeUi.cs, WheelUpgradeWindow.cs, WheelPurchaseUi.cs, and RenderedWindowBackground.cs.
- Added this implementation plan before changing gameplay/UI code.
- Added centralized UI colors/border settings without breaking existing Settings/Statistics behavior.
- Developer confirmed Settings opens/closes normally, Statistics opens/closes normally, and the initial UI settings change did not break anything.
- Implemented the first Buy/Upgrade layering safety step: purchase confirmation uses a blocking full-screen control and cannot pass input through to Upgrade controls underneath.
- Buy and Upgrade windows are mutually exclusive at the interaction level.
- Developer correction received: Upgrade window size is already good and must not be enlarged. Only its font size needs to use UiSettings "Ui Size Medium". Only the Buy window should be enlarged.
- Developer tested the Buy/Upgrade modal behavior: click-through is correctly blocked; opening Buy/Upgrade closes the other window correctly.
- Developer found that outside-click handling initially failed, then confirmed the revised implementation works.
- Final Buy confirmation checkpoint passed: inside click keeps it open; Yes purchases; No closes; outside click closes.
- Developer confirmed Upgrade window outside-click closing works on both Windows and Android.
- Developer explicitly reverted the attempted Settings/Statistics outside-click implementation. Settings and Statistics must NOT close when clicking outside them.
- Added the requirement that the Buy confirmation window be centered around the Buy button/wheel in the same manner as the Upgrade window.
- Developer confirmed the Buy-window click blocking now works reliably every time, including when an Upgrade button is underneath the Buy window.
- Added requirement: clicking BUY or UPGRADE must close an open SETTINGS or STATISTICS window, while the clicked Buy/Upgrade control must still function normally.

SAFETY RULES FOR THIS ATTEMPT
- Small, reviewable commits/changes.
- No Fluid Simulation changes unless required to expose existing state to UI.
- Prefer script-side fixes when the existing Nodes already exist.
- If a required Node, Control, CanvasLayer, or script binding is missing, explicitly ask the developer to create/bind it in Godot.
- Never solve an input problem only by changing visual Z/layer order; hit-testing must also be corrected.
- Never remove or replace the established Settings/Statistics open/close pathway without tracing and preserving it first.
